
const Title = ({name}) => {
  return (
    <div>
      <h2 className="text-2xl font-semibold md:text-5xl text-center mb-4  dark:text-gray-100">
    {name}
      </h2>
    </div>
  );
};

export default Title;
